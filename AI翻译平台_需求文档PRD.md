# AI 多语言翻译校验平台 · 需求文档（PRD）

> 版本：v1.0　｜　类型：内部工具　｜　状态：已上线运行

---

## 一、项目背景

公司运营一款社交 App（含用户资料、关注、私信、聊天、动态、直播、礼物、会员、充值、支付、举报、封禁、申诉、账号安全、运营活动等功能），需要将大量界面文案和运营文案本地化到多种语言。

**痛点**：
- 人工翻译成本高、速度慢，且难以保证术语统一
- 单一机器翻译质量不稳定，占位符（如 `%s`）、格式容易翻错，酿成线上事故
- 缺少质量把关环节，无法批量处理数千条文案

**目标**：搭建一个**傻瓜式、可批量、带质量把关**的翻译平台，让非技术人员上传文件即可完成高质量多语言翻译，并自动筛出需要人工复核的高风险条目。

---

## 二、目标用户

| 角色 | 使用场景 |
|---|---|
| 运营/产品（主用户） | 上传待翻译文案，选语种，一键翻译，导出结果 |
| 复核人员/同事 | 复核平台标记的高风险条目，修正后确认 |

**核心要求**：无需懂技术、无需命令行，全程网页点击操作。

---

## 三、核心功能需求

### 1. 文件导入
- 支持上传 **Excel（.xlsx/.xls）和 CSV** 文件
- 可视化选择"要翻译哪一列"（带首行示例预览）
- 支持中文文件名（自动处理编码，不乱码）

### 2. 参考列（提升准确度）
- 可勾选其他列作为翻译参考，帮助消除歧义
- 参考列分两类，可手动切换：
  - 🌐 **语种译文**：该文案已有的其他语言译文
  - 📝 **场景说明**：文案的使用场景/用途描述

### 3. 多语言翻译
- 支持目标语言：**西班牙语、法语、日语、葡萄牙语、土耳其语**等（可扩展）
- 一次可选多个目标语言批量翻译

### 4. 三模型翻译流水线（质量核心）
```
模型A 独立翻译  ┐
                ├→ 模型C 全上下文审校裁决 → 最终译文 + 质量评分
模型B 独立翻译  ┘
```
- **A、B 两个模型按同一套严格标准独立翻译**（互不可见，交叉验证）
- **C 模型**结合原文、术语表、参考信息，独立审校 A/B 两版，选优/融合/改写，给出译文和评分

### 5. 术语表
- 支持导入术语表（原文→指定译法），翻译时强制统一使用

### 6. 三档智能分流（效率核心）
系统自动把每条译文分到三档，减少人工负担：

| 档位 | 判定 | 处理 |
|---|---|---|
| ✅ 自动通过 | 高分 + 无风险 + 检查通过 | 直接采用 |
| 🔍 运营抽查 | 中等分 / 中风险 | 建议抽查 |
| 👤 人工复审 | 低分 / 高风险 / 硬性错误 | 必须人工确认 |

### 7. 程序检查（一票否决）
翻译后做确定性校验，**优先级高于模型评分**——即使模型给高分，只要以下任一出错，强制转人工：
- 占位符（`%s`/`%d`）数量或对应错误
- HTML 标签、URL 损坏
- 数字、金额被篡改
- 违反术语表
- 译文为空 / 疑似未翻译

### 8. 人工复核台
- 并排展示：原文 / 译文A / 译文B / C推荐译文 / 分歧说明 / 风险等级
- 可修改并确认最终译文
- 支持**导出待复核条目**（含评分、风险、复核原因等），发给同事离线复核

### 9. 结果导出
- 导出 **Excel / CSV**，文件名格式「原名(翻译后).xlsx」
- 待复核清单单独导出，含完整判断依据列

---

## 四、非功能需求

| 项 | 要求 | 现状 |
|---|---|---|
| **性能** | 支持数千条批量翻译 | 约 1 秒/条，3000 条约 50 分钟 |
| **并发** | 多条同时翻译 | 并发 20，滑动窗口调度 |
| **稳定性** | 单条卡住不拖垮整体 | LLM 请求 60 秒超时保护 |
| **大批量** | 3000 条一次上传不卡 | 轻量进度轮询，前端不卡 |
| **容错** | 空单元格/缺列不报错 | 已做防御处理 |
| **易用性** | 网页操作，无需技术背景 | 桌面快捷方式一键启动 |
| **可访问** | 本地 + 公网可访问 | 本地 + Cloudflare 隧道 |

---

## 五、业务流程

```
1. 上传文件（Excel/CSV）
      ↓
2. 选择原文列 + 参考列 + 目标语种
      ↓
3. 点击"开始翻译"
      ↓
4. A/B 双模型独立翻译 → C 审校裁决（自动并发）
      ↓
5. 程序检查 + 三档分流
      ↓
6. 人工复核台处理"人工复审"档条目
      ↓
7. 导出最终结果（Excel/CSV）
```

---

## 六、技术架构（简要）

- **前端**：单页网页（HTML/JS），扁平化 2.0 设计风格
- **后端**：Node.js 服务，进程守护 PM2
- **模型接入**：通过统一代理调用大模型，支持 Anthropic / OpenAI 两种协议，A/B/C 可各自独立配置模型与 Prompt
- **数据**：本地 JSON 存储任务与结果
- **部署**：本地端口 5051 + Cloudflare 隧道公网访问

---

## 七、后续可扩展方向

- 绑定固定域名（免去公网地址变动）
- 断点续传（超大批量中断可恢复）
- Token/成本实时统计
- 更多目标语言、更多术语库来源
- 访问密码保护
