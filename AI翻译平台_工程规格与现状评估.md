# AI 翻译平台 · 工程规格 & 现状评估

> 本文档诚实记录平台的**真实工程实现**，区分「✅已实现 / ⚠️部分实现 / ❌未实现」，用于需求评审和风险决策。基于代码实测，非理想设计。

---

## 一、性能与吞吐

| 项 | 现状 | 说明 |
|---|---|---|
| 单条超时上限 | ✅ **60 秒**（可配 `LLM_TIMEOUT_MS`） | 超时抛错→触发兜底。旧PRD的10s已放宽以兼容慢模型 |
| 并发度 | ✅ **20**（可配 `TRANSLATE_CONCURRENCY`） | 滑动窗口调度，一条完成立刻补下一条 |
| 批次策略 | ✅ 滑动窗口（非固定批次） | 无木桶效应 |
| 单条实测速度 | 全 Claude ≈ **1 秒/条**；含 gpt-5.6-sol ≈ **7-10 秒/条** | 取决于所选模型 |
| **1 万条全量预估** | 全Claude ≈ **8-9 分钟**；慢模型 ≈ **1.5 小时**（单语种，多语种×N） | 同时最多 20×3=60 个在途请求 |

---

## 二、限流与稳定性

| 项 | 现状 | 风险 |
|---|---|---|
| API 主动限流 / QPS 节流 | ❌ **未实现** | 依赖上游代理自扛；并发20下峰值约60 req 在途，可能触发上游限流 |
| 超时/失败重试 | ❌ **无业务重试** | 仅 temperature 被拒时去参数重试一次（非业务重试） |
| 单条卡死保护 | ✅ 60s 超时 | 不会拖垮整个任务 |
| 模型调用失败兜底 | ✅ 有 | 见下方"容错策略" |

> ⚠️ **建议补强**：加令牌桶限流 + 失败指数退避重试（当前完全没有，大批量下遇上游限流会批量失败）。

---

## 三、评分与分流机制

### 评分范围
- `overall_score`：**0–100**（C 终审版输出，分流主用）
- `consistency`：**1–10**（简版输出，缺 overall_score 时 ×10 折算）

### 三档分流阈值（❌ 硬编码，前端不可配，改需动代码）
| 档位 | 判定条件 |
|---|---|
| 👤 人工复审 | 程序检查未过 / score<70 / high风险 / needs_human_review / 占位符·术语·语言检查fail / decision=human_review |
| 🔍 运营抽查 | score 70–84 / medium风险 / decision=rewrite |
| ✅ 自动通过 | score≥85 **且** auto_approve=true **且** 程序检查全过 **且** risk=low（四条全满足） |

### "无风险"定义
C 返回 `risk_level='low'` + 7 项程序检查全过（占位符/HTML/URL/数字/术语/非空/疑似未翻译）。

---

## 四、容错策略（真实行为）

| 场景 | 处理 |
|---|---|
| **模型 A 翻译失败/超时** | 不重试同模型 → 降级用免费引擎(MyMemory)兜底翻译，仍交 C 审校 |
| **模型 B 失败/超时** | 同 A，免费引擎兜底 |
| **模型 C 审校失败/超时** | 规则兜底：**默认采用 A 的译文**，一致性=1 → 强制进人工复核 |
| **整条 A+B+C 都异常** | 用原文占位，标记 error，进人工 |
| 空行 | ✅ 上传时自动过滤 |
| 缺列/空单元格 | ✅ 导出已做防御（补空，不崩溃） |
| 编码/中文文件名 | ✅ 已处理（latin1→utf8、CSV去BOM） |
| **列数校验/格式预检** | ❌ 无主动校验报错，脏数据静默容忍 |

---

## 五、断点续传（重要短板）

- ❌ **完全未实现**
- 一万条跑到 5000 条服务挂了/中断 → **只能从头重来**，已翻的不复用
- 影响：超大批量有重跑风险，且浪费已消耗的 token/成本
- **建议**：按 (taskId, rowIndex, lang) 记录已完成，重启时跳过已译——这是大批量场景的必需功能

---

## 六、安全与权限（重要短板）

| 项 | 现状 | 风险 |
|---|---|---|
| API Key 存储 | 明文存 `translation-settings.json`（已 .gitignore 不进git），API 返回脱敏为 `••••••` | 服务器本地文件明文，有权访问机器者可见 |
| 访问权限控制 | ❌ **无登录、无密码** | 任何能打开网页的人都能用 |
| 模型/Key 配置权限 | ❌ **无权限隔离** | 任何访问者都能进「模型设置」改模型和 Key |
| 公网暴露 | Cloudflare 临时隧道 | 链接泄露=API额度可被他人消耗 |

> ⚠️ **强烈建议**：公网使用前加**访问密码**（至少 Basic Auth），否则 API 额度有被盗刷风险。

---

## 七、优先级建议（按重要性）

| 优先级 | 缺口 | 影响 |
|---|---|---|
| 🔴 P0 | 访问密码/鉴权 | 公网裸奔，额度可被盗刷 |
| 🔴 P0 | 断点续传 | 大批量中断=从头重来 |
| 🟠 P1 | 失败重试 + 限流 | 大批量遇上游限流会批量失败 |
| 🟠 P1 | 分流阈值可配置化 | 现在改阈值要改代码 |
| 🟡 P2 | 文件格式预检报错 | 脏数据静默容忍，用户不知情 |
| 🟡 P2 | Token/成本统计 | 无法量化成本 |

---

## 八、当前技术参数速查

```
并发度        : 20            (TRANSLATE_CONCURRENCY)
单条超时      : 60 秒         (LLM_TIMEOUT_MS)
自动通过线    : overall_score ≥ 85 且 auto_approve 且 low risk 且 程序检查通过
人工复审线    : < 70 分 / 高风险 / 硬性检查失败
评分范围      : overall_score 0-100 ; consistency 1-10
程序检查      : 占位符/HTML/URL/数字/术语/非空/疑似未翻译 (7项, 一票否决)
兜底         : LLM失败→免费引擎(A/B) / 规则裁决默认取A(C)
重试         : 无业务重试
断点续传      : 无
鉴权         : 无
API Key      : 明文存 translation-settings.json (.gitignore)
```
